#include <stdio.h>
void main(){


        float lado, area;

        printf("Escreva o lado do quadrado: ");
        scanf("%f",&lado);

        area = lado * lado;
        printf("A area do quadrado e: %f", area);

}
